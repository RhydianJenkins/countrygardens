import { Box } from '@mui/material';

export const Map = () => {
  return (
    <Box sx={{
      width: '100vw',
      backgroundColor: 'primary.main',
      margin: 'auto',
      padding: '16px',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      Google Map...
    </Box>
  );
}
